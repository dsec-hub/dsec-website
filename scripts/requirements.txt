# Python dependencies for the pixel-illustration pipeline (scripts/).
# Create the env once, then install:
#   python3 -m venv .venv && source .venv/bin/activate
#   pip install -r scripts/requirements.txt
# Pinned (== ) so everyone runs the versions the scripts were written against.
certifi==2026.7.22
charset-normalizer==3.5.1
idna==3.19
numpy==2.5.2
pillow==12.3.0
requests==2.34.2
urllib3==2.7.0
